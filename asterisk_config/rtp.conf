; =====================================================================
; SH-105 — RTP Configuration for Asterisk
; =====================================================================
; Constrains RTP ports to 10000-10099 to match Docker port mapping.
; =====================================================================

[general]
rtpstart=10000
rtpend=10099
